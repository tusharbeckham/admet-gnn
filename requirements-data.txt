# =============================================================
#  ADMETriage -- dataset acquisition ONLY
# =============================================================
#
#  This is a throwaway environment. Create it, download the raw data,
#  then forget it. It is never used for training, export, or serving,
#  and it is never the environment you develop in.
#
#      uv venv .venv-tdc --python 3.12
#      uv pip install --python .venv-tdc/Scripts/python.exe --no-deps PyTDC==1.1.15
#      uv pip install --python .venv-tdc/Scripts/python.exe -r requirements-data.txt
#      .venv-tdc/Scripts/python.exe training/data/download_tdc.py
#
#  ---------------------------------------------------------------------------
#   Why `--no-deps` for PyTDC, and not `-r` alone
#  ---------------------------------------------------------------------------
#   Installing PyTDC with its declared dependencies FAILS on Windows. It pulls
#   `tiledbsoma`, a single-cell genomics package with no Windows wheel, which
#   then tries to compile libtiledbsoma from source and dies:
#
#       Failed to build `tiledbsoma==1.11.4`
#       error MSB4184 / Error: not a CMake build directory (missing CMakeCache.txt)
#       AssertionError: error when building libtiledbsoma from source
#
#   Nothing in this project touches single-cell genomics. `--no-deps` skips that
#   tree, and the packages below are the ones `tdc.benchmark_group.admet_group`
#   genuinely needs -- established by installing, importing, and adding whatever
#   the traceback asked for, one at a time.
#
#   Note the `setuptools<81` pin in particular: PyTDC 1.1.15 calls
#   `pkg_resources.get_distribution(...)` at import time, and setuptools 84
#   REMOVED `pkg_resources` entirely. Unpinned, the import fails with
#   `ModuleNotFoundError: No module named 'pkg_resources'`, which reads like a
#   broken virtualenv rather than a deleted API.
#
#   `rdkit` is deliberately NOT here. The hard rule below means this environment
#   performs no chemistry, so PyTDC's `rdkit<2024.3.1` pin never has to be
#   satisfied -- and the version conflict that motivated splitting the two
#   environments simply does not arise.
#
#      deactivate
#
#  If rdkit has no wheel for 3.12 at this old pin, use --python 3.11 for
#  THIS environment only. It is isolated, so the version it runs on has
#  no bearing on the training environment. That freedom is the entire
#  reason for keeping the two apart.
#
# -------------------------------------------------------------
#  Why this file exists
# -------------------------------------------------------------
#  PyTDC 1.1.15 pins rdkit>=2023.9.5,<2024.3.1, while current rdkit is
#  2026.03.x. It also pulls in the HuggingFace training stack
#  (transformers, accelerate, datasets, evaluate, huggingface-hub) and
#  two single-cell genomics packages (cellxgene-census, tiledbsoma).
#  None of that belongs in a training environment whose only job is to
#  produce one ONNX file.
#  Ref: github.com/mims-harvard/TDC/issues/374
#
#  Keeping TDC here lets requirements.txt stay on a current rdkit with a
#  small, auditable dependency tree.
#
# -------------------------------------------------------------
#  HARD RULE for anything run in this environment
# -------------------------------------------------------------
#  Write raw CSV and nothing else. No canonicalisation, no salt
#  stripping, no InChIKey generation, no scaffold assignment, no
#  featurisation. Those steps belong in the training environment against
#  one pinned rdkit version.
#
#  Rationale: features computed by rdkit 2023.09 and features computed
#  by rdkit 2026.03 can differ in edge cases (aromaticity perception,
#  ring finding, stereo handling). If half the pipeline runs on each,
#  the training set and the serving path disagree in ways that no test
#  catches and no metric explains. One rdkit, one feature contract.
#
#  TDC's own get_split(method='scaffold') is a CROSS-CHECK only. The
#  authoritative split is training/data/scaffold_split.py, run in the
#  training environment, because the Rust side must reproduce that exact
#  grouping.
# =============================================================

# PyTDC itself is deliberately NOT listed here.
#
# Listing it means `uv pip install -r requirements-data.txt` re-resolves its
# declared dependencies, drags `tiledbsoma` back in, fails to build it, and
# ABORTS THE WHOLE INSTALL -- so pandas never lands either and the first error you
# see is `ModuleNotFoundError: No module named 'pandas'`, which points at the
# wrong problem entirely. Install it separately with `--no-deps`, per the header.
#
# What follows is the set PyTDC actually needs in order to reach `admet_group`,
# established empirically -- install, import, add whatever the traceback asks for,
# repeat -- rather than taken from its metadata.
pandas>=2.2
numpy
requests
tqdm
fuzzywuzzy
packaging
scikit-learn
huggingface_hub

# `pkg_resources` is imported by tdc/oracles.py at import time and was REMOVED
# in setuptools 84. Without this pin the whole package fails to import.
setuptools<81

